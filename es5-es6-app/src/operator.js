/// operators

// == and ==== 

let x=10;
let y='10';
let z= x==y;
let t=x===y;
console.log(`compare number 10 and string 10 using == ${z} and using === ${t}`);

// || (OR opratore)

let var1 = true;
let var2 = false;
console.log(`Or Opratore ${var1 || var2}`);

let myLocation = myLocation || 'Mumbai';
console.log(`My Location is : ${myLocation}`);

// ? : (trinary Opratore)

let val1='Hello';
let result = val1 ? 'Good' : 'Bad';
console.log(`Result ${result}`);

// typeof (Operator)

let price = 100;
let product = 'iPhone';
console.log(`Product is ${product} with ${typeof product} type and price ${price} with ${typeof price} type.`);


